# Generated by iptables-save
*raw
:PREROUTING ACCEPT [0:0]
:OUTPUT ACCEPT [0:0]
-A PREROUTING -i fwbr+ -j CT --zone 1
COMMIT

*filter
:INPUT ACCEPT [0:0]
:FORWARD ACCEPT [0:0]
:OUTPUT ACCEPT [0:0]
COMMIT

*nat
:PREROUTING ACCEPT [0:0]
:INPUT ACCEPT [0:0]
:OUTPUT ACCEPT [0:0]
:POSTROUTING ACCEPT [0:0]

# Log HTTP (port 80) requests (limit 5/min to avoid spam)
-A PREROUTING -p tcp -m tcp --dport 80 -m limit --limit 5/min -j LOG --log-prefix "REAL-IP: "

# Log HTTPS (port 443) requests (limit 5/min to avoid spam)
-A PREROUTING -p tcp -m tcp --dport 443 -m limit --limit 5/min -j LOG --log-prefix "REAL-IP: "

# NAT (Masquerade for outgoing traffic)
-A POSTROUTING -s 10.10.1.0/24 -o wlo1 -j MASQUERADE
COMMIT
